# Rebuilt inside the image — copying the host's would ship darwin binaries.
node_modules
.next
out

# Regenerated by `prisma generate` during the build.
src/generated

# Secrets stay out of the image; compose passes them in at run time.
.env
.env.*
!.env.example

.git
.gitignore
*.tsbuildinfo
npm-debug.log*
.pnpm-debug.log*
.DS_Store

# Not needed to build or run the app.
docker-compose.yml
Dockerfile
.dockerignore
README.md
AGENTS.md
CLAUDE.md
